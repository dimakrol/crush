// Placeholder shell. The react-admin application (dataProvider, authProvider,
// resources, dashboard) replaces this in phase 5 — it exists now so the dev
// process tree and the production build are wired end to end from phase 0.
export function App() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: 24 }}>
      <h1>Crash Pilot — Backoffice</h1>
      <p>Skeleton is up. UI arrives in phase 5.</p>
    </main>
  );
}
